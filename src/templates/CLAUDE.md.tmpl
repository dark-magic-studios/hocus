# Claude Code instructions

Read `AGENTS.md` in this same directory for full project context. This file
exists only because Claude Code looks for `CLAUDE.md` specifically; the
content lives in one place to avoid the two files drifting out of sync.
